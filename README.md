# LineART - Project Management System

Система управления проектами для инженерных команд.

## 🚀 Быстрый старт (локально)

```bash
# Установка зависимостей
npm install

# Запуск сервера
npm start

# Откройте http://localhost:3000
```

## ☁️ Деплой на Render.com

### Шаг 1: Подготовка GitHub репозитория

```bash
# Инициализация git (если ещё нет)
git init

# Добавление файлов
git add .

# Первый коммит
git commit -m "Initial commit"

# Создайте репозиторий на GitHub и подключите:
git remote add origin https://github.com/YOUR_USERNAME/lineart.git
git push -u origin main
```

### Шаг 2: Деплой на Render

1. Зайдите на [render.com](https://render.com) и создайте аккаунт
2. Нажмите **"New" → "Blueprint"**
3. Подключите ваш GitHub репозиторий
4. Render автоматически найдёт `render.yaml` и настроит сервис
5. Нажмите **"Apply"**

### Шаг 3: Настройка переменных окружения (опционально)

В панели Render перейдите в **Environment** и добавьте:

| Переменная | Значение | Описание |
|------------|----------|----------|
| `NODE_ENV` | `production` | Режим работы |
| `GOOGLE_DRIVE_FOLDER_ID` | `your_folder_id` | ID папки Google Drive |

## 📁 Структура проекта

```
lineart/
├── server.js           # Express сервер
├── index.html          # Главная страница
├── login.html          # Страница входа
├── style.css           # Стили
├── js/                 # JavaScript модули
│   ├── app.js          # Главный модуль
│   ├── projects.js     # Управление проектами
│   ├── calendar.js     # Календарь
│   └── ...
├── server/             # Серверные модули
│   ├── googleDrive.js  # Google Drive интеграция
│   ├── telegramBot.js  # Telegram уведомления
│   └── archiver.js     # Архивация проектов
├── projects/           # Данные проектов (создаётся автоматически)
├── render.yaml         # Конфигурация Render
└── package.json        # Зависимости
```

## 🔐 Безопасность

**ВАЖНО:** Не загружайте в git:

- `creds.json` - учётные данные пользователей
- `google-credentials.json` - ключи Google API
- `telegram-config.json` - токен Telegram бота

Эти файлы указаны в `.gitignore`.

## 🛠️ Технологии

- **Backend:** Node.js, Express
- **Frontend:** Vanilla JS, CSS
- **Интеграции:** Telegram Bot, Google Drive
- **Карты:** Leaflet.js
- **Графики:** Chart.js

## 📞 Поддержка

По вопросам обращайтесь к разработчику.

---

**Версия:** 1.0.0  
**Лицензия:** MIT
